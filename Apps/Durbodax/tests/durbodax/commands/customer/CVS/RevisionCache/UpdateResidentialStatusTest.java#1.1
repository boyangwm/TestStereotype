package durbodax.commands.customer;

import durbodax.customers.CustomerData;
import durbodax.customers.Enums.ResidentialStatus;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author Kirk
 */
public class UpdateResidentialStatusTest {

    public UpdateResidentialStatusTest() {
    }

    @BeforeClass
    public static void setUpClass() throws Exception {
    }

    @AfterClass
    public static void tearDownClass() throws Exception {
    }

    @Before
    public void setUp() {
    }

    @After
    public void tearDown() {
    }

    /**
     * Test of execute method, of class UpdateResidentialStatus.
     */
    @Test
    public void testExecute() {
        System.out.println("execute");
        String[] input = new String[] { "command", "5" };
        UpdateResidentialStatus instance = new UpdateResidentialStatus();
        instance.execute(input);

        assertEquals(5, CustomerData.CUSTOMER.getCustomer().getResidentialStatus());
        assertEquals(ResidentialStatus.DIFF_STATE, CustomerData.CUSTOMER.getCustomer().getResidentialStatusEnum());
        
    }

}